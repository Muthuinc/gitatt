pipeline {
    agent any

    stages {
        stage ('first') {
            steps{
                sh ' ./muthu.sh'
            }
        }

        stage ('second') {
            steps{
                sh ' ./muthu.sh'
            }
        }
    }
}